# Custom Components

This folder contains shareable Home Assistant custom integrations.

## Included

- `notion_travel/` - YAML-configured integration that syncs Notion trip + child databases into HA sensors.

## Publishing rules

- Keep secrets and DB IDs out of committed files.
- Prefer schema-tolerant parsing and defensive defaults.
- Include a package README for each component.
